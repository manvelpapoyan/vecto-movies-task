'use client';
import styled from 'styled-components';

export const Main = styled.main`
  width: 100%;
  max-width: 1920px;
  display: flex;
  flex-direction: column;
  align-items: center;

  margin: 0 auto;
`;
